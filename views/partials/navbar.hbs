<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <i class="bi bi-calendar-event"></i> Manager de Eventos
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{#if isHome}}active{{/if}}" href="/">
                        <i class="bi bi-house"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{#if isMisEntradas}}active{{/if}}" href="/mis-entradas">
                        <i class="bi bi-ticket-perforated"></i> Mis Entradas
                    </a>
                </li>
                {{#if user}}
                {{#if (eq user.rol 'ADMIN')}}
                <li class="nav-item">
                    <a class="nav-link {{#if isValidar}}active{{/if}}" href="/validar-entradas">
                        <i class="bi bi-check-circle"></i> Validar Entradas
                    </a>
                </li>
                {{/if}}
                {{/if}}
            </ul>
            {{#if user}}
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle"></i> {{user.nombre}} {{user.apellido}}
                        {{#if (eq user.rol 'ADMIN')}}
                        <span class="badge bg-warning text-dark">ADMIN</span>
                        {{/if}}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><span class="dropdown-item-text"><small>{{user.email}}</small></span></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="?admin={{#if (eq user.rol 'ADMIN')}}false{{else}}true{{/if}}">
                            {{#if (eq user.rol 'ADMIN')}}
                            <i class="bi bi-person"></i> Cambiar a Usuario Normal
                            {{else}}
                            <i class="bi bi-shield-check"></i> Cambiar a Admin
                            {{/if}}
                        </a></li>
                    </ul>
                </li>
            </ul>
            {{/if}}
        </div>
    </div>
</nav>
